spring:
  profiles:
    active: dev
---
spring:
  profiles: dev
  application:
    name: zuul
server:
  port: 6001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka,http://localhost:10087/eureka
---
spring:
  profiles: test
  application:
    name: zuul
server:
  port: 6002
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka,http://localhost:10087/eureka
  
