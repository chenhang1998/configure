server:
  port: 10086

spring:
  profiles: Eureka

eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka
    register-with-eureka: false
    fetch-registry: false
---
server:
  port: 8081

spring:
  profiles: test

eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka
  
